# 所有模式都加载。带 VITE_ 前缀 → 会注入到客户端代码。
VITE_APP_NAME=环境变量演示

# 故意不带前缀：模拟后端密钥。它不会进客户端代码（import.meta.env 读不到），
# 也不会出现在构建产物里。这是 Vite 防泄露的安全闸门。
SECRET_TOKEN=this-should-never-reach-the-browser

# 不带前缀，业务代码读不到；但 vite.config.ts 用 loadEnv 能读到，
# 再通过 define 注入为编译期常量 __APP_VERSION__（演示「配置侧读 env」）。
APP_VERSION=1.0.0
